# Full local install for the Knowledge Base QA System.
#   python -m venv .venv && source .venv/bin/activate   (Windows: .venv\Scripts\activate)
#   pip install -r requirements.txt && pip install -e .
# On Colab use requirements_colab.txt (does NOT pin torch).

# --- core ---
numpy>=1.24
pandas>=2.0
PyYAML>=6.0
scikit-learn>=1.3
rank-bm25>=0.2.2
pydantic>=2.5

# --- deep learning / NLP ---
torch>=2.2
transformers>=4.46
datasets>=2.20
accelerate>=0.34
sentence-transformers>=3.0
sentencepiece>=0.1.99
evaluate>=0.4

# --- vector index ---
faiss-cpu>=1.8

# --- serving / API / UI ---
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
gradio>=4.44
prometheus-client>=0.20

# --- reporting (auto report + slides) ---
reportlab>=4.1
python-pptx>=0.6.23
matplotlib>=3.8

# --- tuning + optional LLM brain ---
optuna>=3.6
anthropic>=0.39

# --- dev / tests ---
pytest>=8.0
