# Colab-safe requirements for the Knowledge Base QA System.
# Does NOT list torch (Colab ships a CUDA-matched build — reinstalling torch is
# the #1 cause of CUDA-mismatch errors). The notebook installs this and leaves
# torch untouched.

transformers>=4.46
datasets>=2.20
accelerate>=0.34
sentence-transformers>=3.0
sentencepiece>=0.1.99
evaluate>=0.4
scikit-learn>=1.3
rank-bm25>=0.2.2
pandas>=2.0
PyYAML>=6.0

# vector index
faiss-cpu>=1.8

# serving / API / UI (for the in-notebook demo)
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
gradio>=4.44
pydantic>=2.5

# reporting (autopilot report + slides)
reportlab>=4.1
python-pptx>=0.6.23
matplotlib>=3.8

# tuning + optional LLM brain
optuna>=3.6
anthropic>=0.39
